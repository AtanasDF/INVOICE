// The one name the app goes by (item 26 on Atanas's list): the tab, the
// header, the sign-in line, the home-screen label, emails and files all read
// it here. public/manifest.json and public/sw.js can't import it, so
// test-site-name holds them to it.
export const SITE_NAME = "Invoiceover";
