# Tonight's list — 22 September 2026

Made at 03:45 with Atanas, from everything open: `notes/backlog.md`, `notes/tonight.md`,
`notes/claude-notes.md`, the open items in `CLAUDE.md` and `SESSIONS.md`, the first-page
research, his four Settings screenshots and the voice notes with them, and tonight's asks:
feedback by email, sign-in before scanning, a confirmation email on sign-up. Grown at 04:10
when he asked for a big list ("you're gonna be working a lot"): every item that has work
behind it now carries its steps, lettered, in the order to do them, each ending with the
effort and the test in brackets, and "Atanas:" where only he can act. Eighty-seven items
in seven sections, numbered straight through, with 110 lettered steps under them (42a, 42b and 42c count as items of their own, so 90 numbered lines); section 7 is what
the sweep of the signed-in pages turned up. Nothing here needs a migration except the one
option under item 3 and the paywall (56). Tick items off here as they land, with the commit.

Decisions are taken as recommended, per his standing "always do as recommended"; each is one
line to change if he says otherwise. Two things to test everything against, from his 04:10
ask: a postcode typed in gives a list of addresses, and an address typed in gives its
postcode; triple-tested, on Hidefield if need be, adding and changing but never deleting
what was there.

## 1. Feedback
What exists: a Feedback pill on every signed-in page, a /feedback page (category, message, page, time) saving to a `feedback` table; each person sees only their own notes. Missing: nothing reaches Atanas (his account cannot even read other people's rows — only the service role can), no email, no session check, no handled mark, no `notes/feedback-inbox.md`.

1. **✓ done 6dc5d30 (email per note; the receiving address is his account's).** Email each feedback to Atanas the moment it is sent, through Resend, with the sender's address and page (an hour; Atanas: which address receives it).
   a. A feedback route, signed-in only: it takes the message (up to 5,000 characters), category and page, saves the row as the sender (same rules as today, no service role), then emails the maker's address through Resend from feedback@invoiceover.com with Reply going to the sender; subject "Feedback: category — page"; body carries the message, the sender's address and id, the page, the time in London, the browser and the deployed commit. If the email key or the maker's address is missing, or Resend fails, the row is still saved and the answer says "not emailed" — the start-of-session read (item 2) catches what the email missed. 20 an hour per person. A route rather than a database trigger: a trigger would need an extension and the Resend key kept in the database, fires with no log anyone reads, and cannot run under the harness (two hours; test: new suite test-feedback, and the route-guards suite gains the feedback route as signed-in only; Atanas: the receiving address).
   b. The Feedback page posts to that route instead of writing the table itself; intro becomes "Spotted something wrong, confusing or missing? Tell us — it goes straight to the maker."; after a send, "Sent. Thank you." in a status line and the note at the top of the list; a failed send keeps the typed text and says so in the usual words (an hour; test: the page half of test-feedback).
   c. New logic suite test-feedback-email, recompiled from the app's own email-text code every run: subject holds category and page, HTML escapes scripts and quotes, the text part carries the message whole, reply-to is the sender, a 5,000-character message survives, an empty category reads "Other", the time prints in London (an hour; test: the suite itself, in the nightly run).
   d. New suite test-feedback: the route against the mock database and a stand-in for Resend that records what it was sent — a stranger gets 401, a fake-token caller's row lands with their id, the stand-in saw to/from/reply-to/subject and the message, 5,001 characters are refused in plain words, Resend answering 500 or the key being empty still saves the row and says "not emailed"; then the page half — the form sends, says "Sent. Thank you.", lists the note first, and a failed send keeps the text and shows the alert (two hours; test: the suite, green four-at-a-time and one-at-a-time).
   e. The receiving address goes into Vercel Production as FEEDBACK_TO (an address, not a secret, but the choice is his), redeploy, then one live send from the test account: it lands at that address, Resend shows it Delivered from feedback@invoiceover.com, Reply is addressed to atanaschoo@gmail.com, and the row is in the feedback table; CLAUDE.md's list of environment names gains the two new ones (minutes; test: live only, and the Vercel listing shows the name; Atanas: the address, and its inbox if it is Hidefield's).
2. **✓ done 6dc5d30 (script; needs the service key in web/.env.local).** Start-of-session check: read new rows with the service role, write them into notes/feedback-inbox.md (an hour).
   a. A small script in the harness folder, no packages: reads the project address and the service key from the local env file (never printed), fetches every feedback row oldest first, maps ids to email addresses through the admin users list, and appends to `notes/feedback-inbox.md` one unticked line per row not already in the file — short id, London date, address, category, page, message — printing only "N new". A documented SQL fallback for a machine without the key (it needs Atanas signed in and his Chrome, which is why the script comes first). CLAUDE.md rule 9 gains "then run the inbox script" after git pull (two hours; test: new suite test-feedback-inbox; live: run it once against the real project — the file's rows equal the table, and no key fragment appears in the file).
   b. New suite test-feedback-inbox: a stand-in database and users list with three rows; run the script twice into a scratch file — three lines, then no change; tick one by hand, add a fourth row, run again — the tick stays, the fourth is appended, no duplicates; the output never contains the key; the dates are London dates with the clock pinned the way test-midnight does it (an hour; test: the suite itself).
   c. Where the file may live: it holds testers' words and email addresses. If the repo is private, commit it so both sessions and machines share the ticks; if public, ignore it in git, keep it on this Mac only, and say so in CLAUDE.md (minutes; test: git's ignore check matches the decision; Atanas: confirm the repo's visibility — GitHub → the repo → Settings → General shows it).
3. **✓ done 6dc5d30 (tick in the inbox file).** A way to mark feedback handled — a `handled_at` column, or a tick in the inbox file (an hour; migration if in the database; Atanas: which).
   a. Without a migration (recommended, done first): the tick in the inbox file is the record — "[x]" plus "handled <date>: what was done or the commit"; the script never rewrites a line it has already written, so ticks and notes survive every run; whoever holds the repo sees the state (minutes; test: the tick-survives check in test-feedback-inbox; live: tick a real row, run the script, the tick stays; Atanas: says whether the file is enough).
   b. With a migration, only if he wants testers to see "Seen" on their own notes: backup 016 of the feedback table, then migration 036 adding handled-at and a handled note and taking away update and delete from signed-in users (the app never updates or deletes feedback, and without that a tester could tick their own row); verified as rule 3 says, exercised as a signed-in user inside a rolled-back transaction; the script then stamps handled-at with the service role when a tick appears in the file, and the Feedback page shows "Seen"; on a feature branch until the migration is applied (two hours; migration; test: the RLS audit reads the new files, test-feedback gains "a handled note shows Seen"; live: tick a row, run the script, the tester's page shows Seen; Atanas: decides, and runs the SQL in his Chrome with the destructive-operations dialog confirmed by hand).
   c. Close the section: type check, lint, build, the nightly run green with the three new suites in it, one commit on main, push; SESSIONS.md entry, items 1–3 ticked with the commits, `notes/claude-notes.md` gains the feedback address and the start-of-session command (an hour; test: the run exits clean, Vercel deploys green, one live feedback from the test account arrives by email and appears in the inbox file on the next run of the script).

## 2. Accounts
4. **✓ done (scanning only).** Decide the scope: sign-in before scanning only; typing an invoice, Check a company and the address lookup stay open (decision; recommended so, taken).
5. **✓ done 3390ed5.** Scan route refuses strangers; Free page's scan button becomes "Sign in to scan"; "No account needed" reworded; route-guard and free-draft tests and notes updated; one commit (an evening).
   a. The route: anyone without a valid sign-in token gets 401 "Sign in to read an invoice from a photo." before the body is even read; the anonymous path, its per-hour cap, the "Free scanning isn't available" and "or sign up" lines go; 60 an hour per person and 200 an hour overall stay; engine still optional, Gemini by default; the new-invoice page already sends its token and needs no change (an hour; test: test-route-guards moves the route from open-by-design to signed-in-only, so a bare call and a made-up token both get 401 or 403; live: the same route on the live site answers 401 with no header and "No file was provided." with the test account's token).
   b. The Free page, signed out: the scan card's button becomes "Sign in to scan" and no camera control is rendered; tapping it keeps the draft on the device and goes to sign-in with a note to come back to the Free page and open the scanner; on return the page waits for the sign-in to load, opens the capture screen once (on the iPhone-camera path the card stays with its button focused and "Signed in — tap to scan", since a file picker cannot open itself) and tidies the address so a reload does not reopen it; a 401 mid-scan says "Your sign-in has lapsed. Sign in again to scan." (two hours; test: test-free-draft — after typing a name the card reads "Sign in to scan", pressing it lands on the sign-in page with the return address, signing in through the mock and coming back keeps the typed name, drops the chooser and shows the capture screen, and the database log still holds no writes; live: signed out on the live Free page, sign in as the test account, back with the scanner open, a synthetic photo fills the editor).
   c. Words: "No account needed" goes from both places it lives; the scan card's sub-line adds "You'll be asked to sign in first — each reading costs us."; the tip says the same; the sign-in page opened with a return address says "Sign in and we'll take you straight back to your invoice."; CLAUDE.md, `notes/claude-notes.md` and this list stop saying the route is open (an hour; test: test-free-draft asserts "No account" is gone and the take-you-back line shows; test-labels stays green; live: read the Free page signed out).
   d. The five camera suites that reach the camera through the Free page signed out (autozoom, autozoom-out, camera-tip, pinch, tips) plant the mock's fake session before pressing the scan button, and the shared camera launcher blocks every request to the real Supabase host, so a planted session can never talk to the real project (an hour; test: tips and camera-tip green in the nightly run, the other three singly with their clips; the run's output shows no blocked-request line and no call to the real project).
   e. Close: type check, lint, build, restart the server, the nightly run green, one commit on main saying why scanning now needs a sign-in, push; SESSIONS.md entry and this item ticked (an hour; test: the run exits clean; Vercel deploys green, then the three live checks above repeated on the live site).
   Note: item 18 later renames this button to "Take a photo of an old invoice" on the new start screen and keeps the same sign-in-and-return path; the return parameter that lands first stays, so the two do not fight.
6. Supabase: Site URL and redirect list set to the live site (minutes; Atanas signed in to Supabase, Claude clicks).
   a. Look before touching: with him signed in, read and record in `notes/claude-notes.md` what stands today — Email provider (Confirm email on or off, code expiry, code length), URL Configuration (Site URL and Redirect URLs — a Site URL still on localhost means today's password-reset links already point at localhost), SMTP settings and templates, and the emails-per-hour limit (minutes; test: each screen screenshotted into the scratchpad and the notes match; live: "Forgot password?" on the live sign-in page for atanaschoo@gmail.com produces an email whose link points at the live host; Atanas: signed in to Supabase in his Chrome, he watches).
   b. Set Site URL to the live Vercel address (swapped to invoiceover.com when item 15 lands) and Redirect URLs to the Vercel address, invoiceover.com and localhost, each with a trailing wildcard — without the matching pattern Supabase silently swaps the return address for the Site URL and the way back is lost (minutes; test: live only — a password reset from the live sign-in page arrives with a link to the live host, and a sign-up in item 14 lands where it asked to; Atanas: signed in; Claude fills the fields with him watching).
7. Supabase auth emails through Resend SMTP from invoiceover.com, rate limit checked (an hour; Atanas: creates and pastes the key).
   a. Custom SMTP on: sender accounts@invoiceover.com (any address on the verified domain; the mailbox need not exist), sender name from the site-name decision (26), host smtp.resend.com, port 465, username "resend", password a new Resend key named "Supabase auth" with sending access that Atanas creates and pastes himself; then emails per hour to 100. The built-in sender allows only a few an hour and is for testing, so this comes before Confirm email goes on (an hour; test: live — "Forgot password?" for atanaschoo@gmail.com arrives from accounts@invoiceover.com within a minute and shows Delivered in Resend; the key value appears nowhere in notes or transcript; Atanas: creates the key and pastes it into the SMTP password box; nobody else touches the value).
8. **✓ done (web/supabase/email-templates; pasting is his).** Confirm/reset/change-email wording drafted, copy kept in the repo (an hour; Atanas pastes into the templates).
   a. The wording lives in `notes/auth-emails.md`: Confirm sign-up (a link to the app's confirm page plus "or type this code"), Reset password (to the reset page as today), Change email address — short sentences, the sender name, none of the Free page's banned words; pasted by Atanas or by Claude with him signed in (an hour; test: no suite; live — the first sign-up email in item 14 reads as written, its link opens the confirm page and its code works in the app; Atanas: pastes the templates, or watches).
9. Switch "Confirm email" on, link expiry to 24 hours — last of the dashboard steps (minutes; Atanas).
   a. The switch itself: Confirm email on, code expiry 86400 seconds (24 hours), code length 6 — only after items 5, 10, 13 are live and SMTP (7) is on (minutes; test: item 14; Atanas: the switch).
   b. The order for an unattended night, written here: the app side (10, 13) ships first to main because with Confirm email off a sign-up still returns a session and nothing changes for anyone; URL configuration, SMTP, templates and the switch wait for Atanas; the switch must not go on before the app side is deployed and SMTP is on, or new sign-ups get the old one-line message, a link to the Site URL, and lose their way back (minutes; test: none — this note and the close in 13c).
10. **✓ done 6e78ead (app side; the Supabase switch is his).** App side: link comes back to the app, a "We've sent you an email" screen with Send it again, plain words for "email not confirmed" and expired links (an evening).
   a. The sign-in page: sign-up passes a return address made of the page's own address plus the "next" it was opened with, so the Free page's scanner request survives the email round trip; a new "sent" screen replaces "Account created. Check your email…": heading "Check your email", "We've sent a link to <address>. Tap it and you're in — or type the 6-digit code from the same email below", a code box, "Send it again", "Already have a sign-in? Sign in", and "Come back to this screen after tapping the link"; the address is kept on the device so a reload keeps the screen; an address already registered gets the same screen and nothing reveals that (two hours; test: test-sign-up, item 13; live: item 14).
   b. Auth errors in plain words, keyed on Supabase's error codes: wrong password → "Wrong email or password."; not confirmed → the sent screen with "You haven't confirmed <address> yet. Look for our email (spam folder too) or send it again"; too many emails → "Give it a minute, then try again."; expired code or link → "That link or code has expired or was already used. Send a new one."; weak password → "Choose a longer password."; anything else the caller's usual fallback. Today the page shows Supabase's own "Email not confirmed" and "Invalid login credentials" word for word (an hour; test: test-sign-up — a faked not-confirmed answer shows the sent screen naming the address, a faked wrong-password answer shows "Wrong email or password.", and the raw words never appear; live: a wrong password on the live sign-in page).
   c. A confirm page, public like the reset page: reads the token, the type and the return address from the link, shows one button "Confirm my email" and only on the tap uses the token — so a link scanner (Gmail preview, Outlook SafeLinks) fetching the address cannot burn it — then goes to the return address; an expired token shows "This link has expired or was already used" with an email box (prefilled) and Send it again; a link missing its parts says "This link is missing something. Open it from the email again."; the Confirm-signup template points at this page (two hours; test: test-sign-up — a good token lands on the Free page signed in with the scanner request, a bad one shows the expired screen and Send it again posts a resend, no parts shows the missing-something words, the database log has no writes; live: the tap-the-link step of item 14).
   d. The default link keeps working too: Supabase's own confirmation link lands on the sign-in page with the session in the address (the app picks it up on load and sends the signed-in visitor on), and a used or expired one lands with an error code — the sign-in page reads the code from either place and shows the expired wording with Send it again, so whichever template Atanas pastes nobody meets a blank page (an hour; test: test-sign-up — a fake session in the address ends on the Free page signed in; an expired-link address shows the expired words and the resend control, never the raw error text; live: tap a confirmation link twice in item 14).
11. **✓ done (code box).** Decide: link only, or also a 6-digit code typed in the app — recommended both, folds into 10 (decision; taken: both, code first).
   a. Why the code matters: on an iPhone a link tapped in Mail opens Safari, not the installed home-screen app, and Safari's storage is not the app's — a draft made inside the app is not on the page the link opens; the code typed into the app's own "sent" screen is the path that keeps the draft, so the screen leads with the code when it is running as the installed app, and item 12's phone test goes by that (minutes; test: test-sign-up with standalone display emulated — the code box comes before the link sentence; live: the phone step of item 14).
12. **✓ done (the link comes back to the page they came from).** Free-page draft survives the sign-up detour; tested on the phone (an hour).
   a. The draft is written to the device before the detour (5b), the return address carries the scanner request (10a), and on the phone the code path (11a) keeps it inside the installed app (an hour; test: test-sign-up's right-code check finds the Free draft still on the device; live: item 14 (iv) on the Mac and (vi) on the phone; Atanas: the phone).
13. **✓ done (test-sign-up; the code box waits for the email template, 8).** Harness suite for sign-up, resend, code, unconfirmed sign-in (an evening).
   a. New suite test-sign-up, in the nightly run: a signed-in launch with an intercept answering sign-up with a user and no session (exactly what Confirm email does), resend once fine then "too many", verify with a session for code 123456 or the good token and expired otherwise, and password sign-in with "not confirmed" for the unconfirmed address; checks: the sent screen names the address, the sign-up request carries the return address with the scanner request, Send it again works then says wait a minute, a wrong code keeps the screen, the right code lands on the return address with the Free draft still on the device, the confirm-page and address-session cases from 10c and 10d, and no database writes throughout (two hours; test: the suite, green four-at-a-time and one-at-a-time).
   b. Close: type check, lint, build, the nightly run green with test-sign-up in it, commit on main (no schema), push; SESSIONS.md, and items 8, 10, 11, 12, 13 ticked as far as they went, 6, 7, 9, 14 marked "waits for Atanas" with the exact dashboard paths beside them (an hour; test: the run exits clean; Vercel deploys green; the live sign-in page shows "Wrong email or password." for a wrong password — the one live-visible change while Confirm email is still off).
14. Live proof with atanaschoo+signup1@gmail.com, never Hidefield (an hour; Atanas: inbox and phone).
   a. In this order, only after the app steps are live and SMTP is on and the switch (9a) is on: (i) type a draft on the live Free page signed out, press Sign in to scan, sign up as atanaschoo+signup1@gmail.com → the sent screen; (ii) the email within a minute; Send it again → a second; a third at once → "give it a minute"; (iii) sign in with +signup1 before confirming → the "not confirmed yet" screen; (iv) tap the link on the Mac → the Free page signed in with the scanner request and the draft still there; (v) tap it again → the expired screen; (vi) on the iPhone, inside the installed app, sign up +signup2 and type the code → still in the app, draft kept; (vii) both accounts stay (Atanas may remove them under Authentication → Users; never Claude); Hidefield untouched throughout (an hour; test: the plan itself — every step screenshotted into the scratchpad, outcomes in SESSIONS.md; Atanas: the Gmail inbox, the iPhone for (vi), and the switch).
15. Put the app on invoiceover.com before confirmation links go out; email DNS untouched (an hour; Atanas: root or app. host).

## 3. The first page
16. **✓ done (asked at the photo button).** Decide how sign-in-before-scan fits a page that bans "account" and "sign up" — recommended: ask at the photo choice, not on the first page (decision; taken).
   a. The sign-in ask sits on the photo button of the start screen (18a): signed out, "Take a photo of an old invoice" is a plain link to sign-in that comes back to the start screen with the photo choice, sub-line "You sign in first. It is free, and your invoice stays here." (sign in is allowed on the page; account and sign up are not); signed in it is today's capture button; back from sign-in the start screen shows with the photo button focused and never opens the camera itself, because iOS needs the tap. Lands in one commit with 5a (the route refusing strangers) if 5 has not landed first (an evening; test: test-first-page — signed out the photo control is a link whose return address is the start screen with the photo choice and no camera or file picker appears; signed in through the mock, pressing it mounts the capture screen; test-route-guards as in 5a; test-free-draft: a stranger still writes nothing; live: private window, tap the photo button, land on Sign in, sign in as the test account, come back to the start screen).
17. **✓ done a037dbb (without the picture).** Build the recommended page: headline, one line, two full-width buttons, foot line; static paint, no dashboard code, own title, suite (an evening).
   a. Move the dashboard out of the home page into its own component unchanged; the home page becomes a server page rendering a small switch: signed in shows the dashboard, signed out shows the first page; the gate treats the home address as public so it no longer bounces a stranger to sign-in (an evening; test: test-first-page — signed-out home stays home, no redirect, shows the headline and never "Loading…"; signed in through the mock, home shows the Dashboard heading and not the headline; the fourteen dashboard suites that open home signed in stay green; live: a private window shows the first page, his signed-in phone still opens on the dashboard).
   b. No flash for the signed-in and no dashboard code for strangers: a tiny script in the page reads the stored session before paint and marks the page, styles hide the first page when marked, and the dashboard is loaded only once the user is known (two hours; test: test-first-page — with a fake session on the device the first-page section is already hidden at load and no first-page words are on screen before the Dashboard heading; as a stranger the database log has no request at all; test-weight gains a check that a stranger's home fetches no dashboard, tax-card or OpenCV code and its JavaScript is well under the Free page's 1.5 MB, printing the figure; live: reload the dashboard on the phone three times and never see the first page).
   c. The page paints from the server's HTML: headline, one line, two plain links to the Free page and the company check, foot line, nothing that waits for React or data (an hour; test: test-first-page — a plain fetch of home, no browser, returns HTML holding the headline and both links; with JavaScript off a click on Make an invoice lands on the Free page; live: fetch the site from the command line and see the buttons in the HTML).
   d. The words, exactly as the research: heading "Make an invoice. Check a company.", line "Both are free. Nothing to join. Nothing to pay.", button one "Make an invoice" with "Say what you did and what it costs. Then print it or email it.", button two "Check a company" with "See if a company is real, still trading, and who runs it." and small "The facts come from Companies House.", foot "We do not keep anything you type unless you ask us to." (an hour; test: test-first-page — the seven strings in that order, exactly one heading, exactly one dark button, both buttons sentence case starting with a verb; test-free-draft's nothing-written check is what makes the foot line true; live: read it on the phone).
   e. Header and sign-in link: signed out, the header shows only the site name (to home) and Sign in; the Free invoice and Check a company header links go because the page's buttons replace them; sign-out still lands on the sign-in page; the not-found page's "Back to the dashboard" becomes "Back to the start" (an hour; test: test-first-page — signed out the header nav has exactly one link, Sign in; signed in the nav is unchanged; test-sign-out green; test-check-company's count of two company-check links on the Free page retrained to check the link on home instead; live: a private window shows Sign in top right only).
   f. Title, description, robots: home gets the title "Make an invoice. Check a company. — <site name>" and the description "Make an invoice and print it or email it, or check if a UK company is real and who runs it. Both are free."; home is indexed; the dashboard sets the tab to "Dashboard — <site name>" once shown so the signed-in tab is not the marketing line; the sign-in page gets noindex like the private invoice pages; the Free page gets its own title and description instead of inheriting the root's (an hour; test: test-first-page — signed out on home the tab title starts with the headline, the description is the sentence and there is no noindex; the sign-in page has noindex; signed in on home the tab starts with Dashboard; the Free page has its own title; live: view source on the three addresses).
   g. Notes and log: CLAUDE.md gets the first page (where it lives, the gate exception, the no-flash trick, sign-in before scan, the harness launchers now signing in), the two new suites and the retrained labels; SESSIONS.md entry as each step lands; items 16–29 ticked with commits; push after each step (minutes; test: git log shows one push per landed step; the top of SESSIONS.md names every suite touched; type check, lint and build clean before each push; live: the next session reads it cold and finds nothing missing).
18. **✓ done 690e95d.** "How would you like to start?" screen with two big buttons; retrain the eleven suites that press today's labels (an hour).
   a. The Free page's start stage loses both cards (Start blank with its two buttons and 31-word blurb; Scan an existing invoice with its engine picker), the scan tip and the "New customer? Check the company first" line, and gets a heading, two 64-pixel buttons "Take a photo of an old invoice" (sub-line "We copy your details in. You check them. Your phone will ask to use the camera.") and "Type it in" ("Fill in a few boxes. We build the invoice as you go."), a small Back link to home and a small Start a quote link; the engine picker stays only on the pages stage for signed-in users; the header line becomes "Make an invoice or a quote. Print it, or email it." (two hours; test: test-first-page — signed out with an empty device the heading shows, exactly two buttons of 64 pixels or more, no note, no "Start blank"; Type it in opens the editor; Start a quote opens it in quote mode; Back lands on home; test-tips' first seven checks retrained to expect no note on the start screen; live: open the Free page in a private window).
   b. Retrain the eleven suites and the helper that press today's labels: "Start blank" becomes "Type it in" in test-company, test-free-draft, test-share; "Start a quote" is now a link and the click helper already matches links, so test-address, test-address-fields, test-free-quote only need the new page shape; "Scan an existing invoice" becomes "Take a photo of an old invoice" in test-autozoom, test-autozoom-out, test-camera-tip, test-pinch, test-tips and the shared camera opener, which carries twelve more camera suites (an hour; test: every suite named green in the nightly run, the camera ones with their clips regenerated first, the torch ones on their own).
   c. Camera suites sign in before they scan: the mock's Supabase answering is pulled into one shared piece both camera launchers use, seeding the fake session, so the seventeen camera suites still reach the camera through the Free page once strangers cannot (two hours; test: both launchers reach a live video on the Free page; tips, camera-tip, pinch, autozoom and autozoom-out green in the nightly run; bent and far green on their own clips).
19. **✓ done (four boxes, Add more details, Print in the phone bar).** Editor opens with four fields, the rest under "Add more details", Print visible on a phone (an evening).
   a. Four boxes: "Your name or business" (company lookup kept), "Who it is for" (lookup kept), "What you did" (the first line's description), "How much" (the first line's price, quantity stays 1), then "Add another line" and a button "Add more details" that brings in the rest (document type and layout, the rest of the business details, number, date, due date, terms, customer address and email, options, payment details, notes, signature, footer); it opens by itself when a resumed or scanned draft has anything in a folded field, and stays open for the session (an evening; test: test-first-page — after Type it in exactly four visible text boxes plus the two buttons; typing all four changes the preview total; Add more details reveals invoice number, due date and payment details; a stored draft with a sort code reloads with the section open; a scanned template opens it too; retrained where a folded field is filled: test-free-draft's four label patterns, test-share, test-free-quote, test-company, test-address and test-address-fields press Add more details first; the placeholders "Description of the work", "0.00", "INV-001", "Q-001" are kept; live: type an invoice on the phone in four boxes and print it).
   b. The phone bar shows "Print or save" without pressing More: bar = Print or save, Preview/Edit, More (Send or share, Next invoice, Keep it in the app, Start over); desktop's "Print or save as PDF" becomes "Print or save"; the send panel's "Download PDF" reads "Download" on the Free page while the app's invoice page keeps its word (an hour; test: test-first-page — at phone width a "Print or save" button is visible in the fixed bar and pressing it calls a stubbed print; test-one-handed's nothing-covered check stays green; test-share and test-free-quote press "Download"; live: the phone shows Print or save at the bottom of the editor).
20. **✓ done.** "What is the company called?" screen; accept hyphens and any case; new no-match wording (an hour).
   a. The words: heading "What is the company called?", label "Company name, or its number", placeholder "Smith Building Ltd, or 01234567", one 64-pixel button Check, helper "Not sure of the spelling? Type what you have. We show the close matches."; the intro paragraph and the "Limited companies and LLPs only…" line go; no match "We could not find that name. Check the spelling, or type the company number. Only limited companies are on the list."; unknown number "No company has that number. Check it, or type the name instead."; outage "Companies House is not answering just now. Try again in a minute, or look the company up on the official site."; the busy line and the three link buttons lose "register" ("Look it up on the official site"); the no-key card reads "This check is coming soon. For now you can look the company up on the official site."; the page description becomes "See if a UK company is real, still trading, filing on time, and who runs it. Free." (two hours; test: test-check-company retrained for the coming-soon, official-site, could-not-find and is-not-answering lines; test-register-outage reread against the new outage line; test-first-page checks the heading, label, placeholder, helper and the button's height; live: open the check page on the site — the coming-soon card, since there is no key yet).
   b. Hyphens, dots, spaces, any case and a missing leading zero in a company number: spaces, dots, hyphens and slashes are stripped and the rest uppercased before matching; names go to Companies House untouched (its search is case-blind, and stripping would break Rolls-Royce) (an hour; test: test-check-company — "1234-5678" and "1234 5678" open the report for 12345678; "9876543" opens 09876543; "ACME-building" lists the three matches and the recorded upstream query still carries the hyphen; the check route called with a dotted number gives the report; live: once the key is in, type Hidefield's number with a space in it).
   c. The report's screen words lose "register" and "officers": the Officers card becomes "Who runs it" (note "three people run it now, two have left"), "Registered office" → "Official address", "Registered in" → "Country", "The trades this company registered itself under" → "The trades this company says it does", "On the register:" → "Listed website:", the summary "Active, filing on time, three officers, registered for six years." → "Active, filing on time, three people run it, going for six years.", "charges registered against it" → "charges against it", "Registered care of" → "Address is care of"; the copied plain-text report keeps Companies House's own words because it goes to accountants (two hours; test: test-check-company retrained — the people-run-it pattern, the exact summary, the liquidation and address checks; the copied-text check unchanged and still passing; live: none until the key exists).
21. **✓ done in part (sizes and focus rings; the VoiceOver read-through and Lighthouse are still to do).** Accessibility checklist on all three screens: 18px text, 64px buttons, focus rings, 320px, Lighthouse (an evening; Atanas: VoiceOver read-through).
   a. Sizes on the first page: heading 32 pixels or more, body 18–19 with line height 1.5, nothing under 16, buttons 64 tall and full width with 16 between, all in rem so 200% zoom scales them (an hour; test: test-first-page at phone width — computed sizes as listed, each button's height and width, the gap, and with the page's base size doubled still no sideways scroll and both buttons keep their words; live: pinch-free reading on the phone; iOS's largest text size still fits).
   b. Layout: one column, both buttons above the fold on a 375×667 phone, the picture (when it exists) to the right of the words from 768 pixels, no sideways scroll at 320, nothing moving (an hour; test: test-first-page — the second button's bottom within 667; at 320 the page is no wider than the screen and no element's right edge passes it; at 1024 the words and the figure sit side by side; nothing animates on load; live: the iPhone SE in his drawer if he has one, else the 320 check stands).
   c. Contrast: every text on the page at least 7:1 against the background (neutral-600 or darker, no light grey body copy), in dark mode too since the page sets its own background (minutes; test: test-first-page reuses test-dark-mode's contrast expression with the bar raised to 7, light and emulated dark, zero elements under; live: look at it with the phone in dark mode).
   d. Focus and keyboard: a 3-pixel dark focus ring with a 2-pixel offset on every link and button, tab order Sign in then Make an invoice then Check a company, Enter on a focused button navigates (an hour; test: test-first-page — press Tab three times and read the focused text each time in that order; each ring at least 2 pixels and 3:1 against the page; Enter on Make an invoice lands on the Free page; test-labels' pass over the page finds no unnamed button or link; live: Atanas tabs through on the Mac; Atanas: the VoiceOver read-through on the phone).
   e. Lighthouse once, not in the nightly run: against the built server on home, the Free page and the company check (accessibility, best practices, SEO, headless), fix whatever it names, record the scores in SESSIONS.md (an hour; test: accessibility 100 on all three, no SEO or best-practice failure on home; reports kept in the scratchpad; live: the same run against the live site after deploy).
22. **✓ done (the stranger-facing sentences; the editor's own labels stay).** Banned words off the Free page and the company-check page (two hours).
   a. Every hit found tonight, by screen: the Free page's loading line (PDF, account); the builder's too-large message, "Print or save as PDF", "Save to your account", the header line, the scan tip and "Scan an existing invoice"; the editor's "VAT registered" and its description, the reverse-charge text, "CIS subcontractor" and description, "CIS rate", "VAT rate", the footer hint naming UTR, company and VAT numbers, and the UTR and VAT number labels; the send panel's "Download PDF", "needs a free account… Sign in or sign up to send", "your account email", "goes as a PDF"; the check page's description (register, account); the checker's four "the register" lines, its intro, and the no-key card (register, API, key); the report's four register lines and two officers lines ("Accounts" as a filing's name is kept); the report builder's six register and officers lines; the copied text (kept); the sign-in page (kept: it is not a stranger page). The rule: everything a stranger reads before choosing is clean (first page, start screen, company search screen, the four-box editor and its bar); inside Add more details the tax words stay as labels because a VAT number is called a VAT number, but the sentences are rewritten ("Charge VAT" with "Adds a VAT column and your VAT number.", "Works under CIS" with "Keeps labour and materials apart and shows what is held back."); "Save to your account" becomes "Keep it in the app"; the send panel's ask becomes "Email needs a free sign-in, so every email comes from a real person. This invoice stays as it is." with the button "Sign in to send" (two hours; test: new suite test-plain-words — loads home signed out, the start screen, the company search screen and the four-box editor and fails on PDF, scan, upload, template, generate, account, sign up, register, VAT, CIS, UTR, officers, API, instantly, easy as whole words in any case, register in any sense, and asserts Add more details is closed when it reads; "Save to your account" is pressed by test-one-handed and test-free-quote — both retrained; live: read the four screens in a private window).
23. **✓ done (test-plain-words).** Reading-age and banned-word check in the harness (an hour).
   a. test-plain-words also splits each screen's text into sentences and prints a Flesch-Kincaid grade: at most 5 on home (reading age about 10; the research's target of 9 is grade 4 — report the real number), at most 6 on the start and company screens; no sentence over 15 words on home; no "please", "e.g." or "i.e." on the three; every paragraph no wider than 32 times its font size (about 60 characters a line); both new suites in the nightly run (an hour; test: test-plain-words green with the grades printed; the nightly list names test-first-page and test-plain-words and the whole run is green four-at-a-time and one-at-a-time; live: paste the page's words into Hemingway once and note the grade in SESSIONS.md).
24. Companies House key so Check a company works live (minutes; Atanas registers, adds to Vercel by pipe).
   a. In his Chrome, with Claude opening each page: the Companies House developer site → Sign in / Register (a Companies House account: email, verification link, password) → Manage applications → Add an application (name from the site-name decision, environment Live) → Create new key (name "Invoicer app - Vercel", type REST, no IP or domain restriction since Vercel's addresses vary) → Copy; then Claude adds it to Vercel Production and the local env file through a pipe from the clipboard, clears the clipboard, confirms the name is present (never the value), lists the production names, and pushes an empty commit to redeploy (minutes; test: live — the company-search route on the live site says configured, a search for "hidefield" lists his company, Settings shows the lookup placeholder instead of a plain box; harness: test-check-company unchanged; Atanas: registering and signing in, creating the application and key, pasting the key into the pipe — Claude never handles the value).
   b. The harness stays honest once real keys sit in the local env file: test-check-company's off case sets the key to an empty value instead of deleting it (Next loads the file underneath and an empty value wins), test-address-lookup does the same for the address key, both on cases use the fixture key; the harness README says so (an hour; test: both suites green with a dummy key line temporarily in the env file, the not-configured checks still passing).
25. **✓ done (coming soon).** Decide: show Check a company before the key exists, or "coming soon" (decision; taken: shown now, with "coming soon" wording).
   a. The first page shows the button now, the company screen says coming soon until the key is set; when the key is in, the same page works live with nothing else to change (minutes; test: test-check-company's without-key pass covers the coming-soon card and that the official-site link searches; live with the key: check one real company — Hidefield's own number — from the first page and note it in SESSIONS.md; Atanas: the key, item 24).
26. **✓ done 63e84ba (Invoiceover, one constant; his to change in one line).** Pick one site name (tab, header, manifest, sign-in, domain differ) (decision; taken: "Invoiceover", from the domain).
   a. One name in one constant, used by the tab title, the header, the sign-in line, the manifest (which becomes a generated file) and the service worker; today they read Invoice & Expenses, Invoicer, Invoice and Invoicer (an hour; test: test-first-page — the tab title, the header brand text and the name in the manifest all carry the same name; a search of the app shows no other spelling; live: the tab, the header and Add to Home Screen on the phone show one name; Atanas: the final name — the constant takes it in one line).
27. **✓ done (Make).** Pick the verb: Make or Write an invoice (decision; taken: Make).
   a. "Make" on the first page and the start screen; the Add sheet's "Write an invoice" and "Write a quote" become "Make an invoice" and "Make a quote"; page headings "New invoice" stay (minutes; test: test-quote-not-invoice and test-settings-add press the new labels; test-first-page asserts the button text; live: the + Add sheet on the phone; Atanas: the verb, if he prefers Write — one constant). See also 81.
28. The picture: photo or drawing, then make it; page ships without it if not ready (decision + an evening; taken: ships without it, drawing drawn as the candidate).
   a. A picture slot with the research's alt text, rendered only when the asset exists, the page complete without it; then the drawing (a phone held over a sheet, green outline, grey lines for text, no words, no icons, under 20 KB inline) as the candidate (an evening; test: test-first-page — if a picture is on the page it has the alt text "A phone held over a paper invoice. The screen shows the page found with a green outline.", holds no text element and no readable words, and is under 20 KB; every other check passes with the slot empty; live: Atanas says photo or drawing after seeing the drawing on his phone; Atanas: a photo needs his phone and a paper invoice on a plain table).
29. Try the page on three people, two questions (Atanas).
   a. The two questions ("What does this do?" and "What would you press?") and a results table with three rows (one older person, one child, one non-technical adult, on a phone) added as section 7 of `notes/first-page-research.md` (minutes; test: the section exists and the rows are filled in; whatever they said drives the next round of wording; Atanas: the three people and his phone).

29b. **✓ done (/copy).** **Scan anything, free** (Atanas, 04:50: "something exciting"). On the free page, a second scanner button that scans any files at all, not only invoices: photos or PDFs, several pages, straightened and cropped by the scanner the app already has, joined into one PDF, then downloaded, shared, or emailed to someone straight from the page. Free, and for now with no daily limit at all (Atanas, 05:10: "keep it open and let users scan as many scans as they want. Not 23, as many as they want today"; the 23 he first said is kept here in case it comes back). Steps: a. the button and its screen, reusing DocumentCapture's batch mode and BatchReview (an evening; test: new suite test-scan-anything); b. pages to one PDF with pdf-lib, page order and rotation kept, download and share sheet (two hours; test: the suite checks the PDF's page count); c. send by email through the send route, signed-in only as today, the PDF attached, the sender's own address in Reply-to (two hours; test: the send route's suite gains the case); d. no cap for now; only the reader's existing per-address limit against runaway loops stays (nothing to build; if a cap comes back: hit_rate_limit with a new key and a plain "That's enough for today, more tomorrow"); e. the button's words for a stranger, per the research's rules (minutes). Decision: strangers too, or signed-in only (recommended signed-in only, as item 4).

## 4. Settings page

**His voice notes, typed out at 04:00:**
- Expense categories should follow the kind of account (personal, sole trader, limited company) and change with it. Proposal below.
- Invoice numbering: one line. No essay.
- VAT: one switch, with or without VAT, then the VAT number. No explanation.
- Address finder "doesn't work at all". Reproduced on the live site: SE18 1HU lists nothing but the postcode; tapping it fills only the town and postcode. "149 Benares Road" lists the street, then schools in Devon and Hampshire. Cause: OpenStreetMap, the free source, holds no houses for most UK postcodes; Royal Mail's file (paid) holds every address. Decision below.
- Company name: type three letters and a list appears, narrowing as you type; picking one fills the company number; a full company number fills the name. The first two are built and work behind the Companies House key, which is not set on the live site, so today the boxes are plain. Number-to-name to check and add.
- Check a company: names, numbers and the facts appear in the app, with a link to the government page; today, without the key, the page can only send people to the government's own search.
- At 04:10, the two things it must do: type a postcode and get a list of addresses; type an address and get its postcode. Triple-test everything, Hidefield allowed, add and change but never delete what was there.

**Categories by account kind, proposal.** The kind picked at the top of Settings chooses the starting list. A list someone has already changed is kept as it is; changing the kind offers "Switch to the sole-trader list", which adds what is missing and never removes anything (receipts keep whatever category they have). Mileage stays its own thing.
- Personal: Groceries, Household, Rent or mortgage, Bills, Gas & Electric, Transport & Taxis, Fuel, Travel, Meals & eating out, Clothes, Health, Childcare, Subscriptions, Gifts, Other.
- Sole trader, matching the boxes on the self-assessment form: Materials & stock, Subcontractors, Staff wages, Fuel & mileage, Travel & hotels, Meals when away, Rent, rates & utilities, Repairs, Phone & internet, Office & stationery, Software & subscriptions, Advertising, Bank charges & interest, Accountant & legal, Insurance, Tools & equipment, Training, Other.
- Limited company: the sole-trader list plus Salaries & PAYE, Pension contributions, Director's expenses, Client entertaining (not allowable, kept apart so the accountant sees it).

**Facts the steps rest on.** Categories are one list of words on the business profile (empty means the built-in list); a receipt keeps its category as plain text, so no list change can touch a saved receipt. The kind is a text on the profile, limited, sole trader or personal, blank meaning not said. A logo address has had a column since the second migration. The photo bucket's owner rules go by the owner's folder, so a logo can live there. Nothing in this section needs a migration. New card order: Your business (kind first) · VAT · Invoice numbering · Bank details · Payment reminders · Expense categories · Notifications · Email import · Your account (Sign out last); one form wraps the first eight, with a sticky Save bar. The new browser suite is test-settings-cards; the new pure suites are test-category-sets and test-address-filter. Two suites are stale (test-address-signed and test-company look for a "Find address" placeholder and an address box the address fields no longer render) and are not in the nightly run; test-settings-add has never been in it either.

30. **✓ done f70d483.** Copy fixes: stale logo sentence, "no delete button", Notifications lists all four pushes, address not "(optional)" for VAT/limited (minutes). VAT card becomes one switch and the number box; numbering becomes one line (an hour).
   a. First: a feature branch from main, build, run the fifteen suites that touch Settings for a baseline, and put test-settings-add into the nightly list (an hour; test: the nightly run green before any change; test-settings-add appears in the output and passes).
   b. The line under the heading becomes "Fill this in once and it goes on every invoice, quote and reminder. Your account is at the bottom." (minutes; test: test-settings-cards — the paragraph after the heading is exactly that).
   c. Your business, kind first: the three radios keep their words; the address label follows kind and VAT: limited, or VAT on → "Business address" with no "(optional)"; sole trader with VAT off → "Business address (optional)"; personal → "Your address (optional)" (an hour; test: test-settings-cards — all four combinations give those labels; test-settings-add's address-label pattern widened to allow the missing "(optional)").
   d. Your business copy: the name hint reads "The name customers know you by — the headline on every document." plus, for limited, "If the register has a different name, put that below."; the registered box reads "Companies House needs the registered name and number on your invoices. They print small at the foot; your trading name stays the headline." (minutes; test: test-settings-cards — both sentences for limited, the second absent for sole trader; test-settings-add's label checks unchanged).
   e. Delete the stale sentence "A logo can go here too once file storage is set up — not yet, so this is text-only for now." (the upload is item 40) (minutes; test: test-settings-cards — the page holds neither "file storage" nor "text-only").
   f. VAT card: heading "VAT", no paragraph; one switch "VAT registered"; the "VAT number" box shows only while the switch is on, with the note "9 digits, with or without GB in front — 123 4567 89"; a stored number is kept when the switch goes off (an hour; test: test-settings-cards — off → no number box; on → the box with that note; type a number, Save, switch off, Save → the row still holds the number with VAT off; test-keyboards passes).
   g. Invoice numbering becomes one line: "Your next invoice will be INV-124. It goes up by one each time an invoice is sent." above Prefix and Next number; the went-backwards warning stays (minutes; test: test-numbering, test-quiet-failures; test-settings-cards — the card's paragraph has no "override" and at most two sentences).
   h. Bank details becomes one line: "Printed on every invoice under "How to pay"." with the box's placeholder unchanged (minutes; test: test-settings-cards; test-quiet-failures still finds the box by a label matching "bank").
   i. Your account: "Closing the account" becomes "You can't delete the account from inside the app, on purpose: a record thrown away by mistake can't be got back, and HMRC expects six years. Ask through Feedback and it's done by hand, after you've exported what you want to keep." (the old "no delete button anywhere in this app" was untrue — drafts and receipts can be deleted); Sign out is the card's last control (minutes; test: test-settings-add's "no delete button" check changed to the new phrase; test-settings-cards — "Sign out" is the last button).
31. **✓ done f70d483.** Neutral colours: radios and checkboxes, eleven red Remove links, red Regenerate button (minutes).
   a. Radios and checkboxes take the dark neutral accent; the eleven category Remove links become grey underlined text; Regenerate address loses its red for the plain bordered button; the amber "Treat this address like a password" line becomes grey and medium weight; red stays only on alert errors (minutes; test: test-settings-cards — no red hue in any button, link or input accent on Settings outside an alert; test-dark-mode passes). The rest of the app: 67.
32. **✓ done f70d483.** Cards in a new user's order, account and Sign out last; dashboard reminder switch moved to Notifications (minutes).
   a. One form wraps the first eight cards, the account card moves to the bottom with Sign out as the page's last control; heading order Your business, VAT, Invoice numbering, Bank details, Payment reminders, Expense categories, Notifications, Email import, Your account (an hour; test: test-settings-cards — headings in that order and the last button is Sign out; test-settings-add still passes, it finds the account section by text).
   b. Notifications card: the copy becomes "A push once a day when an invoice is overdue, a bill is due within three days or a recurring expense is due — and straight away when a customer opens an invoice or answers a quote, or a supplier prices a request."; the dashboard checkbox moves here inside the form, relabelled "Also show a reminder on the dashboard when an invoice is overdue" (an hour; test: test-settings-cards — the checkbox under the Notifications heading; untick + Save turns the setting off and the dashboard banner is gone with an overdue invoice seeded; test-settings-add passes).
33. **✓ done f70d483.** Feedback pill stops covering text and Save (an hour).
   a. The rule: the pill never sits over a control — the page body ends with clear space under the pill (plus the phone's safe area) and any sticky bar leaves the pill's column empty; the Free-page phone exception stays (an hour; test: new suite test-feedback-pill — every page in test-fit-sweep's list scrolled to the bottom, the pill overlaps no button, box or link; Settings with the sticky bar showing, the same; test-fit-sweep passes). Every list page: 75.
34. **✓ done f70d483.** Sticky Save when something changed, unsaved-changes guard, instant cards say so (two hours).
   a. Sticky Save: a snapshot of the form at load; changed = the form differs from it; a bar at the bottom shows only while changed, saving, or for two seconds after a save with "Saved." in a status line; "Unsaved changes" beside the Save button; the invoice-counter guard untouched (two hours; test: test-settings-cards — no bar on load; typing in Bank details puts a Save button in view while scrolled to the top; Save → "Saved." in a status region and the bar goes; test-half-saved, test-quiet-failures and test-announced still click Save and pass).
   b. Unsaved-changes guard: the browser's leave warning while changed; a click on any in-app link (header included) and the back button ask "Leave Settings without saving? Your changes will be lost."; a clean page never asks (an hour; test: test-settings-cards — changed + confirm answered no + click Invoices in the header → still on Settings; answered yes → leaves; clean → no ask; test-sign-out unchanged).
   c. Instant cards say so: Notifications, Email import and Your account each get "Buttons here act straight away — there's nothing to save." (minutes; test: test-settings-cards — the line under exactly those three headings).
35. **✓ done f70d483.** Reorder arrows big enough to tap (an hour).
   a. Each arrow becomes a 40×40 bordered button, side by side before the name, named "Move Fuel up" / "Move Fuel down"; Remove sits at the row's end; the row still fits a phone with a long name (an hour; test: test-one-handed — the arrows now count as buttons and must be 32 pixels or more; test-labels; test-fit-sweep; test-settings-cards asserts every arrow is at least 40×40 and that a click moves the row).
36. **✓ done f70d483.** Payment reminders intro cut to two sentences, rest behind "How it works"; same for VAT and numbering (an hour).
   a. Intro: "Up to five emails go to each client with reminders on: 3 days before the due date, on the day, then 7, 14 and 30 days after. Leave a box blank to use the standard wording."; everything else (sender, replies, bank details, the part-paid rule, the placeholders, the Late Payment Act paragraph) moves into a closed "How it works"; the interest checkbox keeps its label (an hour; test: test-settings-cards — closed on load, the summary reads "How it works", opening reveals the client-name placeholder and "Late Payment of Commercial Debts"; test-labels; test-announced — the five labels still tied to their boxes). VAT and numbering: 30f, 30g.
37. **✓ done.** Export includes recurring invoices, quote requests, reminder history, links (an hour).
   a. The export gains recurring invoices, quote requests with their supplier answers, reminder history and invoice and quote links (tokens included) under their own keys; the "Take it all with you" line lists them and says "including the private links you've shared" (an hour; test: test-settings-add's export check extended — one seeded row of each under its key in the downloaded file; test-stored-photos unchanged; test-two-users — none of the new reads returns another account's rows).
38. **✓ done.** Mileage added to customised category lists when defaults grow (an hour).
   a. Mileage is always in the working list: a customised list that lacks it gets it appended; Settings shows it with the note "Mileage is always here: trips are saved under it."; the receipts filter and the new-receipt select show it (an hour; test: test-settings-cards — a seeded custom list without Mileage shows Mileage on Settings, the new-receipt select and the receipts filter; test-mileage unchanged).
39. **✓ done.** Small: shortened import address with Reveal/Copy on their own row; yellow autofill boxes; VAT number shape note (an hour).
   a. Email import: the address shows as its first five characters and dots, full width, wrapping anywhere; Reveal, Copy and Regenerate sit on their own row beneath; the warning reads "Treat this address like a password: anyone with it can put receipts in your account." (an hour; test: test-settings-cards — at phone width the address block is no wider than the screen, the three buttons share one row, Regenerate has no red; test-fit-sweep and test-labels pass).
   b. Yellow autofill boxes: a global style paints autofilled boxes white with dark text (minutes; test: test-settings-cards — a loaded stylesheet rule mentions autofill, since headless Chrome cannot autofill itself; test-dark-mode passes; live: in his Chrome after autofilling the address).
   c. The VAT number shape note: 30f.
40. **✓ done d949a4f (600 pixels, not 512; the quote and its link too).** Logo upload now storage exists (decision; an evening; taken: yes, built now).
   a. A file box in Your business; PNG or JPEG shrunk to 512 pixels on the device, stored in the owner's folder of the photo bucket, the profile keeps its address; shown on the issued invoice (screen, print, PDF) and the private invoice link; "Remove logo" clears the address, the file stays (an evening; test: new suite test-logo — upload through the box against the storage stand-in from test-stored-photos, the row gets a storage address, the issued invoice page shows the image, the private link too; removing clears the address and deletes nothing from storage; test-print passes; Atanas: his veto if he would rather not have logos).
41. **✓ done (the business cards hide; the kind already picked the categories).** Personal use adapts the cards (decision; an evening; taken: yes, built now).
   a. With Personal picked, "Your business" is headed "About you", and VAT, Invoice numbering, Bank details and Payment reminders each collapse to one line "Not needed for personal use — pick A sole trader or A limited company if you send invoices." unless the card already holds a value (VAT on, bank details, a reminder text, or an invoice exists) (an evening; test: test-settings-cards — personal + empty → those four are one-liners; personal + seeded bank details → the bank card shows; back to sole trader → all four show; test-settings-add's personal checks pass; Atanas: his veto).
42a. **✓ done f70d483.** Categories follow the account kind, as proposed above (an evening; Atanas: agree the three lists — recommended as proposed, built now).
   a. Category sets in the categories code: one list per kind, verbatim from the proposal; defaults for a kind (blank kind → today's list); the working list is the custom list when non-empty, else the kind's defaults; a merge adds what the kind's list lacks, compared trimmed and case-blind; the five pages that use categories pass the account kind; the stored column stays as it is (two hours; test: new pure suite test-category-sets — each kind returns its list, blank gives the old list, a merge never drops an entry and adds only what is missing, Other stays last; test-cis, test-mileage and test-review-fixes still pass).
   b. Switching sets without removing anything: picking a kind while the list is untouched swaps the screen to that kind's list at once (nothing stored until Save); with a customised list a line appears "Add the sole-trader categories? It adds what's missing and removes nothing." with an "Add them" button; "Reset to defaults" becomes "Start again with the sole-trader list" and asks first, naming the custom names that would go (two hours; test: test-settings-cards — seed Fuel and Custom A; pick sole trader → the offer appears; Add them → Fuel, Custom A, then the additions; Save → the stored list equals the screen; the receipts table byte-identical before and after; with an untouched list, picking personal shows the personal list and Save stores it).
   c. Expense categories copy: "These are the choices when you save a receipt or a bill. Removing one leaves the receipts that already use it as they are."; the add box keeps its placeholder (minutes; test: test-settings-cards text check).
42b. **✓ done: c and the no-houses note e3fc8e1; a, d, e, f and g 8678bad (ticked too early at 04:46, when only c was built); h waits for his key.** Address finder: pick the address data (decision below), then the paid lookup goes live behind the key with the free one as fallback; the free fallback stops listing places hundreds of miles away and says plainly when it has no houses for a postcode (an evening; Atanas: account and key).
   a. Scaffolding first: the address route takes stand-in hosts for the three services (defaulting to the live ones, like the company lookup does), and a stand-in server in the harness serves tonight's real replies — SE18 1HU's reverse lookup (47 places, none with that postcode, streets Benares Road SE18 1HS, St. Nicholas Road SE18 1HH, Amar Court SE18 1HL), "149 Benares Road" (the street plus two schools in Devon and Hampshire and a road in W14), SW1A 2AA (10 Downing Street), and Royal Mail's shapes (a postcode list, suggestions, one pick, a no-address code, no balance, bad key) with a call log; the paid service's public test key answers "no balance" tonight, so this is the only way to test the paid path off the internet (an evening; test: new suite test-address-lookup — starts the app on its own port with the stand-ins, the way test-check-company does; the canned cases answer as the route answers today and the log shows every call went to the stand-in).
   b. The address logic joins the pure suites: postcode tidying, town case, town from a postcode (London by district, built-up area over council names), the kinds of hit and street handling, merging, splitting and joining an address, Royal Mail lines; the 22 unit checks the session log mentions lived only in the wiped scratchpad (two hours; test: new suite test-address-logic, no browser).
   c. No more schools in Devon: a number-and-street search keeps only hits whose street or name holds the typed street words, and, when a town is typed, whose town, district, county or region is that town; named places (schools, shops, parks) go unless the typed text matches their name; exact street names first; when nothing survives, "No street called X found; check the spelling or use the postcode". So "149 Benares Road" lists Benares Road, London and nothing in Teignmouth or Petersfield, while "10 Downing Street" with no town still lists the six real ones to pick from (two hours; test: test-address-lookup — canned Benares → one option, none from Devon or Hampshire; Downing with town London → London only; test-address-fields live — the Benares check added, the Downing checks kept; live: Hidefield Settings, street 149 Benares Road, Find: one row, London; nothing saved).
   d. A street gives its postcode when the free data knows it: a street hit keeps its postcode (lines: number and street, town, postcode; note "SE18 1HS is the street's postcode, check it is yours") instead of "add the postcode"; a hit with no postcode is completed by a reverse lookup at its position before it is offered, so a pick always fills the postcode box — his second ask; a typed bare number or flat ("149", "Flat 2") is joined onto a picked street with no digits, so the pick reads "149 Benares Road" (an hour; test: test-address-logic — with and without a postcode, the join rule; test-address-lookup — a street hit without a postcode ends with SE18 1HU in the box after the pick; test-address-fields live — the 149 Benares Road pick puts SE18 1HS in the postcode box and keeps 149 on the street line; live: the same on Hidefield's new-client form, then leave without saving).
   e. Plain words when the free data has no houses for a postcode: the search says so and, from the same reverse call, lists the streets within about 150 m of the postcode's centre (nearest first, up to 8) as "pick your street, then add the number" rows with their own postcodes; the note reads "The free address list has no houses for SE18 1HU. Pick your street below, or type the house and street; the town and postcode go in for you"; the "just the postcode" row stays; a pick fills street, town and postcode and keeps a typed number (two hours; test: test-address-lookup — canned SE18 1HU → three street rows plus the postcode row, the note, the no-houses flag; test-address-fields live — SE18 1HU lists Benares Road; live: Hidefield Settings, postcode SE18 1HU, Find: streets and the note, as Atanas saw it fail tonight).
   f. A whole postcode searches by itself — his first ask: when the postcode box holds a postcode that tidies cleanly, Find runs on leaving the box or after 700 ms without a tap (a number-and-street waits for a short pause too, then searches; the Find button stays for a retry); one request per pause, the server's cache covers a repeat (an hour; test: test-address-fields — type SW1A 2AA, no click, the list appears; test-address-signed — exactly one request for a typed postcode, none more on leaving the box; live: Hidefield Settings, type a postcode, watch the list open on its own).
   g. A capped or failed Royal Mail lookup says so and costs less: the route marks the answer as a fallback when the per-person or overall caps refuse a postcode or the paid service fails, so the box says "Royal Mail lookups used up for now, free list" rather than a plain free note; a bad key, no balance or key limit is remembered ten minutes and goes straight to the free lookup, logged once, so a dead key costs neither a cap hit nor an error line per keystroke; a free answer given in a paid slot is cached as free only (two hours; test: test-address-lookup — a no-balance answer → free plus fallback, the next call in the window never reaches the paid stand-in; the 21st postcode search for one person in five minutes → free plus fallback; a pick after the cap → the box re-searches free and says so; a stranger never reaches the paid service; the key never appears in the page; live: none until the key exists).
   h. The paid path live behind the key (the code exists: postcode lists, suggestions as you type, a pick fetched by its id, caps 20 per five minutes and 100 a day per account, 150 and 400 overall, the free lookup when capped): Atanas opens an Ideal Postcodes account, buys the smallest credit pack, creates a key with a daily limit set and auto top-up off, no address restriction (it is used server-side); Claude adds it to Vercel Production and the local env file by pipe from the clipboard, clears the clipboard, confirms the name only, and pushes an empty commit (an hour; test: test-address-signed already proves the token travels with every search; live: signed in as Hidefield, Settings, SE18 1HU → the full Royal Mail list and the note "Royal Mail addresses"; "10 Downing" → suggestions → pick → postcode filled; signed out on the Free page the list is still the free one; the dashboard shows two credits used; Atanas: decision 58, the account and payment, the key and its daily limit, pasting into the pipe — the value is never seen by Claude).
   i. Proved end to end on the live site, triple: on atanaschoo (saves allowed) a new client with a postcode pick, a street pick and a Royal Mail pick if the key is in, saved and reopened with every field where it was put; on Hidefield only Find and pick on Settings, the new-client form, a quote's customer and a quote request's site address, leaving each page without saving; each run three times (an hour; test: live as described, screenshots in the session log; test-address-fields, test-address-signed (rewritten against the named address boxes and added to the nightly run) and test-address-lookup all green on the same build).
42c. **✓ done f70d483 (number fills the name; the key is his).** Companies House key, so the name list, the number fill and Check a company work live; then number-to-name; then Check a company keeps the facts in the app with a link out to the register (minutes for the key, an evening for the rest; Atanas: registers at the Companies House Developer Hub, Claude guides on screen).
   a. The key: item 24.
   b. The Company number box on Settings (and the Free page's issuer number box) fills the name: a whole number (8 characters, or 6–7 digits padded with the leading zero) asks the register after 400 ms; an empty registered name is filled with the tidy name and the padded number; a name already typed that differs gets "Registered as X, use it" and is never overwritten; the register's status note shows when the company is not active; "No company has that number" otherwise; without the key it stays the plain box it is today; looking never saves (an evening; test: new suite test-company-number-fill against the mock and a stub — the name filled and saved with the padded number, a typed name kept with the offer shown, a dissolved note, the unknown-number wording, lookup off → no request and a plain box; the Free page: the number fills the business name; test-company-number extended: on Settings a mocked answer fills the name and the log has no profile write; live after the key: Hidefield Settings, type his number, the name appears; no Save unless he says).
   c. A full company number typed where the name goes finds the company: the name box (and the invoice and quote contact box) treats a number-shaped query as a number and lists that one company; picking fills name, number and an empty address as a name pick does; a number not on the register shows "No company has the number 01234567" and creates nothing (two hours; test: test-company-picker grows — 01234567 in the invoice contact box → one row → pick fills the name and keeps the number; 1234567 finds the same; 99999999 → the no-company line, clients table unchanged; live after the key: Settings business name, type Hidefield's number → Hidefield Ltd listed).
   d. Client and supplier forms (new and edit) get a Company number box under More details, saved to the client's number column (already there), with the same fill and offer; a register pick still fills it and typing over the name still clears it (two hours; test: test-company-number grows — a number typed in a fresh supplier form fills the name and saves the number; the edit form shows it; clearing saves blank; the typed-over guard still passes; live: on atanaschoo add a supplier by number, reopen, the number is there).
   e. Check a company keeps the facts in the app with a link out: the report already shows status, who runs it, filings, charges, control and a link to the company's own page on Companies House once the key is on — confirmed, and the link added if missing; the way in: "Company 01234567 on the Companies House register" on the client forms and the contact box becomes a link to the check page with that number, and the clients list shows Check beside a company with a number; before the key the page's coming-soon card and official-site link stay (an hour; test: test-company-picker — the line carries the link; test-company-number — the list's Check link for a saved number; test-check-company — a link with a number opens the report, and the report's link has the right address; live after the key: the check page, "Hidefield" → the report, the link opens the same company).
   f. The register list as you type, verified live once the key is on: three letters → rows within a second, narrowing per keystroke (300 ms pause, one request per pause, the previous one dropped), six rows, active companies only, a dissolved company named exactly gets its note; the caps (60 per address per five minutes, 120 anonymous and 180 signed-in shared) sit under the key's 600; one "is it configured" probe per page load; any wording that reads wrong on the phone fixed on the spot (an hour; test: test-company-picker covers the behaviour with a stub, test-register-outage the outage wording; live: Hidefield Settings and new client "hidef" → Hidefield Ltd; new invoice contact box "Lurra" → the restaurant companies; Free page signed out "acme" → rows; the check page "Lurra" → matches then a report).
42. Fill in business details on Hidefield: company number, bank, VAT switch, and the invoice prefix and next number (the notes say 357358) (minutes; Atanas only).
   a. Typed by Atanas with Claude opening Settings in his Chrome and walking him through; the sticky Save shows the moment he changes anything (minutes; test: live — after Save the Hidefield profile shows the values; no invoice is issued there; Atanas: typing his own details).
   b. Live triple-test on Hidefield within tonight's limits: add a category, rename it, move it up and down, remove it, Save each time and reload; the VAT switch on and off restoring the original; a postcode typed gives the list, an address typed gives its postcode, three times each; every change put back exactly as found; nothing issued, nothing deleted that was there before (an hour; test: the checklist on the phone and in Chrome; a before-and-after diff of the Hidefield profile row is empty at the end).
   c. Suites into the nightly run: test-settings-add, test-settings-cards, test-category-sets, test-address-filter, test-address-logic, test-address-lookup, test-company-number-fill, test-feedback-pill, test-logo and the rewritten test-address-signed; test-company rewritten the same way or its Settings part folded into test-settings-cards (an hour; test: the nightly run green four-at-a-time, and once one-at-a-time for the timing-sensitive ones).
   d. Notes and log: CLAUDE.md's data-model section gains the category sets and the kind rule; `notes/claude-notes.md` the new suites and the stale-suite finding; SESSIONS.md after each card; this list ticked with commits; commit and push after every completed card, never to main before the run is green (minutes; test: git log shows one commit per card on the branch; the merge to main is one fast-forward after a green run).

## 5. Scanner
43. **✓ done 5b75192 (eight of nine; the sideways still remains).** Investigate test-far's nine failures: seven "still" checks never calling the ImageCapture stand-in, the dark phone-like object, the white box on a coloured bill; checks or scanner wrong (an evening).
44. **✓ done 8ff3d8b.** "Getting ready" sign while OpenCV downloads on a cold start (an hour; Atanas: his go-ahead, he said hold).
45. **✓ done ec417a6.** VAT worked out from a printed rate when no figure prints, shown as worked out, harness case (an hour).
46. Correct the GO OUTDOORS date 2012 → 2026 on Hidefield (minutes; Atanas).
47. **✓ done.** Decide Gemini reads everything; recommended keep Gemini default, Claude behind the switch (decision; taken). The picker hidden from everyone else: 78.
48. iPhone tries: batch, signature, scan-to-fill, Share, far receipts, Paid moment, badge, the three hold/zoom changes (an hour; Atanas: phone, test account).
49. Offline scan queue (days; Atanas: needs the phone at hand).

## 6. Everything else
50. Get an email-import address on his account (minutes; Atanas).
51. Dashboard JavaScript under 1.5 MB; agree budgets for the stranger pages (an evening). (22/09 afternoon: test-weight 8/8, the dashboard still under.)
   a. The stranger's home page is measured first, as part of 17b: no dashboard, tax-card or OpenCV code, the figure printed by test-weight (folded into 17b; test: test-weight).
52. **✓ done (fixtures remade, crash-after-passes caught, dev-server suites serial).** Suites that fail only four-at-a-time made reliable; harness npm install noted (an hour).
53. **Partly done: /r/ b995fa4 (test-public-request 28/28); /i/ and /q/ open and show the logo d949a4f (test-public-logo 27/27); their Accept/Decline and open counting still only in test-links and test-quote-links, which need a server built against the stand-in.** Tests for the /i/, /q/ and /r/ pages via the mock-server pattern (an evening).
54. **✓ done 63e84ba.** Tidy stale notes: limiter, Anthropic key, Worker, placeholder, 1.1 s, Gemini default (minutes).
   a. Plus the lines that go false tonight: "the template route is unauthenticated", "anonymous callers always get Gemini" in CLAUDE.md, "Anonymous scanning is Gemini-only" in the notes (5c) (minutes; test: a search of the notes finds none of the three).
55. Safari camera permission set once (minutes; Atanas).
56. Paywall design conversation (decision; days; migration).
57. Open Banking provider and design — not now (decision).
58. Royal Mail addresses key (decision; Atanas: key and limits). The steps: 42b h.
59. Three unused exports: remove or keep (decision; minutes).
60. "Keep working" routine: recreate with permissions or drop (decision).
61. Deletions only he may make: old Resend key, Vercel "web" project, iCloud "name 2" copies and leaving iCloud sync, 24 backup tables, Mapbox token (decision; minutes).

## 7. Sweep
What the pass over the signed-in pages found tonight, read against the first-page research's rules (plain words, one verb, neutral greys, every box named, dates people say, nothing under the pill). Numbered on from the six sections; each carries its effort and test.

62. **✓ done bb0db71.** Plain words on the signed-in pages: "Aged receivables" (dashboard) becomes "How late is what you're owed"; the expenses tile "VAT to keep for review" becomes "VAT on those costs"; the expenses heading stops changing ("Monthly expenses", "Custom-range expenses") and reads "Expenses" with the period under it; "Vendor / shop name" (receipt edit) and "Vendor" (review queue) become "Shop or supplier"; the three "Export CSV" buttons (invoices, receipts, clients) become "Download for a spreadsheet"; "add one manually" in three empty states becomes "add one by hand" (the Add sheet's words); "Text" on a client row becomes "Send a text"; the review queue's "check what the AI read off them" becomes "check what was read off them" (an hour; test: test-plain-words (64) fails on the old words; test-empty-account's page-title and empty-state strings updated; live: open Expenses and Receipts on the test account and read the labels).
63. **✓ done.** The research's banned words inside the app: recurring invoices' "Generate now" / "Generating…" / "generates a draft invoice automatically" become "Make it now" / "Making…" / "makes a draft invoice for you each month"; Settings' "Regenerate address", "Get my import address" and "Could not generate an import address." become "Get a new address", "Get an address to email receipts to", "Couldn't make an address"; the Settings sentence naming a migration number becomes plain words ("the kind of business and the registered details need a database update first; everything else is saved"); the "Where your data lives" paragraph drops the database, host, bucket and file-format names for three plain sentences (kept in the EU, only you can read it, photos behind links that expire). VAT, CIS and UTR stay (an hour; test: test-plain-words; test-settings-add, test-quiet-failures and test-clear still green on Settings and recurring invoices).
64. **✓ done (strangers' pages: the research list; signed-in pages: developer words).** New suite test-plain-words for the whole app: through the mock, render test-labels' page list plus the sign-in page, the Free page and the company check, read the text, and fail on any word from the research list (the full list on the two stranger pages and sign-in; minus VAT, CIS and UTR on signed-in pages; "AI" added), printing the page and the sentence; also print, without failing, every sentence over 15 words per page so the reading-age pass (23) has a list to work from (two hours; test: the suite, added to the nightly run; proven by planting one banned word on a page, watching it fail, then removing it).
65. **✓ done b4d163c.** A dashboard empty state that outlives the tip: today an empty account sees three £0.00 tiles, five £0.00 "Aged receivables" rows, three £0.00 "This month" figures and, after the tip's visits run out, no welcome at all. With nothing in the account show one card ("Nothing here yet. Scan a receipt or write an invoice and this page fills in.") and hide the zero cards; with data, hide the lateness card while nothing is outstanding, and drop the "Spent this month" tile or the card's third figure (they show the same number twice) (two hours; test: test-empty-account — set the tip count to its limit before loading home and assert the welcome still shows and no £0.00 rows remain; test-first-week for the populated dashboard; live: sign in to the empty test account and look).
66. **✓ done b4d163c.** Two small gaps: the Quotes empty state ("No quotes yet.") gains a next step ("Make one to price a job before you start."), and the VAT page shows "Loading…" until its data or error arrives — today the figures card is simply absent, so a slow connection shows a page with no figures and no reason (minutes; test: test-empty-account — both strings for Quotes and VAT).
67. **✓ done (neutral greys; test-neutral).** Neutral greys across the app, not only Settings (31): blue action links (dashboard 5, clients 4, invoices 3, receipts 3, files, VAT, recurring "+ Add line", invoice page, quote page, new invoice, new receipt, scan, review, sign-in 2) become dark underlined text; red Remove / Discard / ✕ links become grey (the confirm dialog carries the warning); the blue "emailed receipts waiting" banner becomes the neutral card the receipts page already uses for supplier matches; green "Saved." and the sign-in page's green info become grey; the red "CN" mini-badge becomes a neutral "Credit note" badge. Kept on purpose: status badges (Paid, Overdue, Sent, Part-paid carry meaning), the amber banners, red alert text (two hours; test: new suite test-neutral — every signed-in page through the mock; any visible element coloured blue-ish or red-ish outside alerts, round badges and the amber banners fails, naming the element's text; test-dark-mode green; Atanas: say if the status badges should go grey too — recommended keep them).
68. **✓ done b4d163c.** "break-words" → "wrap-anywhere" in the five places house style forbids it: the receipts page's details (two), the quote page's contact line, the customer picker, the order lists' block (minutes; test: test-long-values — a 60-character unbroken detail value and a long supplier contact, no sideways overflow at 320 pixels).
69. **✓ done b4d163c.** Clock reads left outside the one place that knows the day: the expenses page's month, year and 30-day range defaults come off the UTC clock (at 00:30 BST on the 1st it opens on last month — the exact bug CLAUDE.md documents), the paid celebration's "this month", and the Free draft's year; all three go through the London clock (an hour; test: test-midnight — the expenses page with the clock pinned to 23:30 UTC on 30 September: the Month box reads 2026-10, Year 2026, the custom range ends on the London date; a paid-celebration check for the month boundary).
70. **✓ done 17310db.** Dates printed as 2026-10-01 on screen: the dashboard's "due 2026-10-01", the invoices list (date, due, credit-note rows), the clients' payment history, the files tiles and preview, the mileage list, both recurring lists ("Next: 2026-10-01"), the invoice page's credit notes and the remove-payment confirm, the credit-of select, the issued invoice's credit-note line — all through one short-date helper (the quote-status code has it; it moves to the dates code) (an hour; test: new suite test-plain-dates — seeds one of each record, walks the pages, fails on a year-month-day pattern in visible text outside boxes; test-statement and test-print still green).
71. **✓ done 1ce8987.** Every box has a name and edit forms show their labels: the clients edit form (VAT number, Contact person, Phone, Payment terms, Default currency are placeholders only and go blank once filled), the receipts edit and review forms (date, category, name, total, VAT), both recurring forms, the new-invoice lines, and the From / To date filters on receipts, invoices and files (a date box shows dd/mm/yyyy twice with nothing saying which is which) (two hours; test: test-labels extended from buttons to every box, select and text area — a name from a label or an aria-label, a placeholder alone fails; the filter dates named From and To; test-announced still green).
72. **✓ done 1ce8987.** Contrast and size floor inside the app: light grey body text (the receipts' exchange-rate note and warranty line, paused recurring rows) is 2.5:1 and becomes neutral-600; the 10-pixel "CN" badge and 11-pixel address-finder note become 12 (minutes; test: test-dark-mode's contrast check extended to Receipts with an exchange-rate receipt and a warranty, and Recurring with a paused item, both colour schemes).
73. **✓ done (current page marked, More pages reaches everything, strangers' pages out of the main row).** The header knows where you are and leads everywhere: the current page marked (darker, semibold, announced as current); one name for the first page ("Home" in the nav, "Dashboard" as its heading today); "Free invoice" and "Check a company" dropped from the signed-in header (they are for strangers and sit first) unless he wants them; Mileage, VAT, Recurring, Files and Needs review given a way in from the header (today only via small links on the dashboard and expenses pages) (two hours; test: test-first-week — every signed-in page reachable by clicking header links from home; test-labels for the current-page mark; test-fit-sweep and test-fit-320 for the header at 320; Atanas: say whether Free invoice / Check a company stay in the signed-in header — recommended no).
74. A phone nav: nine header links wrap to three rows on a phone; a bottom bar with Home, Receipts, Invoices, Quotes and More (More holds Clients, Expenses, Settings and the rest) (an evening; test: test-fit-320 and test-one-handed — every bar item hit-tested at phone width, nothing under the Feedback pill; live on his iPhone; Atanas: agree the five, or say the wrapped header is fine).
75. **✓ done (room at the foot on phones).** The Feedback pill on every list page, not only Settings (33): on a phone it sits over the last card's Edit / Remove on invoices, receipts, clients and recurring; the page body gets bottom padding the pill's height on phones, and the pill hides while a sheet or dialog is open (an hour; test: test-one-handed extended — scroll to the bottom of Invoices, Receipts, Clients, Recurring at phone width and hit-test the last card's buttons).
76. **✓ done 1ce8987.** Confirm before a new import address: "Regenerate address" in Settings stops the old address at once with no question, unlike every other destructive tap in the app; it asks first, naming the consequence (minutes; test: test-no-accidents extended to Settings — the dialog names the old address stopping; Cancel leaves it unchanged).
77. **✓ done 1ce8987.** The Overdue tile and banner open the invoices list already filtered: the invoices page reads a status (overdue, to receive) and a client from its address; the dashboard's Overdue tile, the overdue banner's Review and "View all N outstanding" link with it; the clients page's payment history links to the client's invoices (an hour; test: test-first-week — tap the Overdue tile, the status filter reads Overdue and only overdue rows show; test-delight still green).
78. **✓ done 1ce8987.** The scan page's "Read with Claude / Gemini" picker is shown to everyone (47 is the decision): hide it unless the engine has been set on the device or a Settings switch "Reading engine (advanced)" is on, and the line "Gemini read the document — check…" loses the engine's name (an hour; test: test-multi-docs, test-bad-scan and test-settings-add still green — they set the engine on the device, not through the select; test-plain-words finds no engine names on the scan page).
79. **✓ done (Keep handy).** Starring on the receipts page is unexplained: a ★ that toggles and a "Starred only" filter, nothing saying what it is for. Give it a word ("Keep handy" / "Kept") beside the star and in the filter, or drop starring (an hour; test: test-receipts-list — the toggle's name and the filter's label agree; test-labels; Atanas: keep starring or drop it — recommended keep, with the word).
80. **✓ done (Make).** One verb and one Add convention (lands 27 in one commit): the verb appears in the dashboard intro ("create invoices") and tip ("create an invoice"), the invoices intro ("Create and revisit"), the Add sheet ("Write an invoice", "Write a quote"), the quotes list ("New quote", "New request") and the research ("Make an invoice"); and the Quotes list is the one list without the "+ Add" sheet the other three use (minutes; test: test-plain-words looks for the losing verbs; test-settings-add checks the sheet's rows; test-quotes-ux for the quotes list button; Atanas: 27's verb).
81. **✓ done (inside the sheet).** The dashboard action row has four ways in (Scan tile, + Add, Upload, + Add a client or company) where the research says one primary: fold "+ Add a client or company" into the Add sheet as an extra row, keeping Scan, Add and Upload (he asked for Upload in the open) (minutes; test: test-settings-add — the sheet lists the client row; test-empty-account; Atanas: say if the client button should stay outside the sheet).
82. **✓ done 8678bad (test-address-stubbed 37/37: its own dev server with stand-ins for the three services, since the lookups run on the server, not in the browser).** Address finder, his two asks, offline-testable: the same work as 42b c–h, plus an offline suite — test-address-stubbed intercepts the three services in the browser and answers from fixtures, so it runs without the internet (test-address-fields talks to the live services and times out under load) (folded into 42b; test: the list appears with no click; streets listed with a pick filling three fields and keeping a typed number; a street hit without a postcode ends with SE18 1HU in the box; only the road, no schools, and with "London" typed only London; the paid fixtures — a postcode's list, a pick, a capped call falling back; live: type SE18 1HU on the test account's Settings and watch; type "149 Benares Road", London and see the postcode fill).
83. **✓ done (the strapline waits for the site name, 26).** The sign-in page's words and colours (extends 10): "Need an account? Sign up" and "Create your account" become "New here? Make a sign-in" / "Make your sign-in"; the blue "Forgot password?" and green info text go grey; the strapline follows the site-name decision (an hour; test: test-sign-out (visits the page), test-plain-words with the sign-in page on the stranger list, and test-sign-up (13); Atanas: 26's site name).
84. **✓ done.** Settings' own sweep findings folded into section 4: yellow autofill (39b), the Regenerate confirm (76), the pill (33, 75), plain words (63); nothing else new was found there (minutes; test: covered by the items named).
85. **✓ done.** The Quotes list gets the same "+ Add" sheet the other three lists use, so there is one way in everywhere (from 80) (minutes; test: test-quotes-ux — the sheet opens from the quotes list; test-settings-add).
86. **✓ done (every suite from tonight is in run-all.sh).** Every new suite from tonight in one place, so nothing is written and forgotten: test-first-page, test-plain-words, test-sign-up, test-feedback, test-feedback-email, test-feedback-inbox, test-settings-cards, test-category-sets, test-address-filter, test-address-logic, test-address-lookup, test-address-stubbed, test-company-number-fill, test-feedback-pill, test-logo, test-neutral, test-plain-dates, and the rewritten test-address-signed; each added to the nightly list the commit it lands (minutes; test: the nightly list names all eighteen by the end; the run is green four-at-a-time and one-at-a-time).
87. **✓ done (both dozens).** The twelve done unasked — the surprise he asked for at 04:10, picked from this list because each is small, needs no decision and no migration, and has a test: 62 (plain words on the signed-in pages), 65 (a dashboard that is not nine zeros), 66 (the two small gaps), 68 (the five wrap fixes), 69 (the clock reads), 70 (dates people say), 71 (every box named), 72 (contrast and size floor), 76 (confirm before a new import address), 77 (Overdue opens the list filtered), 78 (the engine picker hidden), 30b–i (the Settings words he asked for in his voice notes) (an evening in all; test: each item's own; the list of what landed, with commits, at the top of SESSIONS.md).

## Decisions for Atanas
Scope of sign-in (4, taken as recommended); link or code (11, both, code first); host name (15); sign-in on the first page (16, at the photo choice); Check a company before the key (25, shown now, coming soon); site name (26, Invoiceover); button verb (27, Make); picture (28, ships without, drawing offered); logo (40, yes); personal-use cards (41, yes); category lists (42a, as proposed); scanner "getting ready" go-ahead (44); Gemini for all (47, kept default); paywall (56); banking (57); Royal Mail (58, paid per lookup, his account and key); unused code (59); routine (60); deletions (61); feedback address and handled mark (1, 3); the feedback inbox file — is the repo private or public (2c); status badges grey or kept (67, keep); Free invoice / Check a company in the signed-in header (73, drop); the five bottom-bar items or the wrapped header (74); starring kept with a word or dropped (79, keep); the client button inside the Add sheet or outside (81, inside); the three people for the first page (29).
Only he can do: the Supabase screens (6, 7, 9, the Confirm email switch and the SMTP key), the Resend key for SMTP (7), pasting the email templates (8), the Companies House account and key (24), the Ideal Postcodes account, payment and key (42b h), his own business details on Hidefield (42), the phone tries (12, 14, 48, 55), the VoiceOver read-through (21), the email-import address (50), the GO OUTDOORS date (46), and every deletion (61).
