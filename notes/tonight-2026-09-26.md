# Tonight's list — 2026-09-26

Everything I can do without him, in the order I would do it. His own items
from `notes/atanas-2026-09-26.md` come first because he asked for them.

## A. What he asked for

1. **Scan a document from a company that is not his** — read it, send or download
   it, and create no contact. `/copy` already does most of it; the gap is reaching
   it from the scanner. `receipts.details.noSupplier` already exists.
2. **Passkey / Touch ID sign-in.** Buildable against Supabase; he tests the
   fingerprint on the Mac at the end. Also settle what "sends you to a different
   page" means — the six-digit code screen, or something firing that should not.

## B. The gaps that make "green" not mean "works"

These are why 3,386 passing checks still do not mean the app is proven.

3. **Production error reporting.** If something throws on somebody's phone, nobody
   ever finds out. A `/api/error` endpoint and a client boundary turns an invisible
   failure into a line that can be read. **Half a day, and the highest value on this
   list.**
4. **Rehearse a restore.** 24 `*_backup_*` tables exist and nobody has ever put one
   back. Restore into a scratch table, verify by content both ways.
5. **Run the RLS suites against a real Postgres.** The mock cannot enforce row
   level security, so every "somebody else's account" check is really testing the
   mock. Today's confidence there is thinner than it looks.
6. **Reading accuracy on all ten benchmark documents, both engines.** Only three
   have been run. Costs a little credit.

Not mine: a real iPhone, a real outside user, and his 106 printed documents.

## C. How I work — today gave evidence for all three

7. **Break every check before trusting it.** Three vacuous checks surfaced today;
   two were mine, and one would have been satisfied by deleting the feature it
   guarded.
8. **Read the log before naming a cause.** I blamed a stale `gen/` recompile for
   four crashed suites and wrote it into three files. The cause was a missing
   summary line, and the log said so by being empty — which no module error is.
9. **Do not run anything beside the harness.** My dev servers and recompiles cost
   three crashed suites and about thirty minutes.

## D. Twenty more, concrete

**The scanner, since it is what he uses most**
10. **Say which engine read a document** on the result, so a slow read is never a
    mystery again. His "replying very slow" was invisible for days.
11. **Time-box a read and fall back.** One Claude read took 71 seconds. Give up at
    ~20s and retry on Gemini rather than let somebody hold a phone.
12. **Warm OpenCV wherever a Scan link exists**, not only the dashboard — arriving
    straight at `/scan` from a home-screen shortcut is the slow path.

**Sweeps that would have caught today's bugs earlier**
13. **Name every control on every page.** `test-labels` covers about twenty of
    thirty pages; the unnamed Category select was found by luck, not by it.
14. **Contrast on every page in all six themes.** It currently checks three pages.
    The invisible dates lived on a page it never opened.
15. **Keyboard-only over the newer screens** — `/help`, `/copy`, `/convert`,
    `/money`, `/jobs`.
16. **Every destructive confirm names what goes.** It is a house rule; check it
    actually holds everywhere.
17. **Double-press every once-only action** — turn into invoice, send, mark paid,
    claim deposit. The `disabled`-is-not-a-guard rule has bitten twice.

**Money, which is the part that must not be wrong**
18. **Print the hardest invoice there is** — CIS plus reverse charge plus a deposit
    plus a credit note — and check the paper matches the screen.
19. **Recurring dates across month ends** — monthly on the 31st, February, and the
    BST boundaries.
20. **Non-GBP receipts** — the fx path, the rounding, and what VAT does.
21. **Extend the money invariants** to quotes, deposits and credit notes combined,
    not just invoices and payments.

**The edges nobody has looked at**
22. **The inbox Worker end to end** — an email in, a needs-review row out.
23. **The emails themselves** — reminders and invoice sends, rendered, with no
    unfilled placeholder and the right links.
24. **Public links** — `/i/`, `/q/`, `/r/`: no enumeration, and a guessing attempt
    is rate limited.
25. **No secrets in the server logs.** Grep what is actually logged.
26. **Every API route's auth and limits as a matrix** — one table, so a route added
    without a fence is obvious.

**Tidying that pays for itself**
27. **Duplicated helpers sweep.** `addDays` turned out to be four implementations
    disagreeing. Check `addMonths`, money formatting and date parsing for the same.
28. **Report what each of the 47 stale suites duplicates**, so his delete decision
    is informed. Reporting is mine; deleting stays his.
29. **The dashboard's 1.5 MB of JavaScript** — find what is actually in it.

## Order I would take them

3 (error reporting) → 1 (his scanner ask) → 13, 14 (the sweeps that catch the
class of bug found today) → 10, 11 (scanner) → 4, 5 (restore and real RLS) →
the rest.
